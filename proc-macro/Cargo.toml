[package]
name = "libnord_proc_macro"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libnord_codegen = { path = "../codegen" }

[lib]
proc-macro = true
path = "src/lib.rs"