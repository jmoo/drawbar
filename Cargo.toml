[package]
name = "libnord_tests"
version = "0.1.0"
edition = "2021"
publish = false

[dev-dependencies]
libnord = { path = "lib" }
libnord_codegen = { path = "codegen" }
libnord_proc_macro = { path = "proc-macro" }
regex = "1.8.1"
syn = "2.0.15"

[workspace]
members = [
    "cli",
    "codegen",
    "lib",
    "proc-macro"
]