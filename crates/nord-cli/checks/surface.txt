# Each noun's verb list, in `nord <noun> --help` order. The first line is the
# top level. surface.sh checks every row, so renaming a command fails the check.
#
# piano, program, raw, sample and setlist share the slot verbs because they are
# the same class of storage, addressed the same way.
: inspect verify edit device program setlist live settings sample piano help
device: controls status info recover geometry help
live: get info deps edit help
piano: get put move rename duplicate delete select info deps focus list probe inspect decode edit trim split build rebuild verify help
program: get put move rename duplicate delete select info deps focus list probe edit help
raw: get put move rename duplicate delete select info deps focus list probe help
sample: get put move rename duplicate delete select info deps focus list probe edit decode encode build verify project help
sample project: new help
setlist: get put move rename duplicate delete select info deps focus list probe edit help
settings: get info edit help
