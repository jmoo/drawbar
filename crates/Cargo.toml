[workspace]
members = [
  "drawbar",
  "nord-bits-derive",
  "nord-cli",
  "nord-format",
  "nord-usb",
]
resolver = "2"

[workspace.package]
edition = "2021"
license = "BSD-3-Clause"
# `u64::is_multiple_of` and friends.
rust-version = "1.87"
repository = "https://github.com/jmoo/drawbar"
# Default version for unpublished crates
version = "0.1.0"

# Cargo profiles only take effect at the workspace root.
[profile.release]
lto = true
strip = true

# Optimize wasm package
[profile.web]
codegen-units = 1
inherits = "release"
opt-level = "z"
panic = "abort"
